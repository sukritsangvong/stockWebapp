#!/usr/bin/env python3

# Accepts either `user = ...` or `user_list = [...]`, but if `user = ...` exists, it will ignore `user_list = [...]`

database = 'stocks'
#user = jondich
user_list = ['oac', 'sukritsangvong', 'caldero', 'sangvongp']
